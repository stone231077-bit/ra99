# Промпт для Cursor - Обновление сайта ra99.in.ua

## Задача

Обновить сайт-визитку ra99.in.ua со следующими изменениями:

### 1. Изменение названия
- Заменить все упоминания `ra99.in.io` на `ra99.in.ua` в:
  - `<title>` в `<head>`
  - Логотип в header (`.logo`)
  - Футер (copyright)

### 2. Переключатель языков
- Добавить в header переключатель языков (UA/RU/EN) справа от логотипа
- Кнопки должны быть стилизованы как `.lang-btn` с классами `active` для текущего языка
- При клике на кнопку должен вызываться `translatePage(lang)`
- Добавить стили для `.lang-switcher` и `.lang-btn` в CSS

### 3. Секция "Наша команда"
- Убедиться, что в секции есть 2 карточки:
  - **Влад Світлячок**: +380983234657, Telegram: https://t.me/+380983234657
  - **Ігор Михайлович**: +380676333323, Telegram: https://t.me/+380676333323
- Карточки должны иметь аватары с инициалами (ВС и ІМ)
- Все тексты должны иметь атрибуты `data-i18n` для перевода

### 4. Улучшение секции брендов
- Заменить простые текстовые карточки на карточки с иконками
- Бренды: Schneider Electric, ABB, Konlux, Horos, Firon, Novodvorski
- Использовать эмодзи-иконки: ⚡ 🔌 💡 ✨ 🔦 🌟
- Структура: `.brand-card` → `.brand-icon` + `.brand-name`
- Добавить стили для `.brand-icon` (font-size: 2.5rem)

### 5. Ассортимент товаров
- Создать новую секцию `.products-section` после брендов
- Товары с иконками:
  - Розетки (🔌)
  - Вимикачі (🔘)
  - Світильники (💡)
  - Проводи (🔗)
  - Автоматика (⚙️)
- Структура: `.products-grid` → `.product-item` → `.product-icon` + `h4`
- Все тексты должны иметь `data-i18n` атрибуты

### 6. Обновление адреса
- Изменить адрес на: "Україна, м. Дніпро, проспект Науки (Гагаріна), 153"
- Добавить `data-i18n="location.address"` для перевода

### 7. Переводы (i18n)
- Добавить переводы для всех элементов страницы в `script.js`:
  - Навигация: `nav.home`, `nav.team`, `nav.brands`, `nav.location`, `nav.contact`
  - Hero: `hero.title`, `hero.tagline`
  - Команда: `team.title`, `team.subtitle`, `team.vlad.name`, `team.vlad.role`, `team.igor.name`, `team.igor.role`
  - Бренды: `brands.title`
  - Товары: `products.title`, `products.sockets`, `products.switches`, `products.lights`, `products.wires`, `products.automation`
  - Расположение: `location.title`, `location.address`
  - Контакты: `contact.title`, `contact.description`
  - Форма: `form.name`, `form.phone`, `form.email`, `form.type`, `form.typeSelect`, `form.order`, `form.proposal`, `form.review`, `form.complaint`, `form.other`, `form.message`, `form.submit`
- Переводы должны быть на 3 языка: RU, UA, EN
- Функция `translatePage(lang)` должна обновлять активную кнопку языка

### 8. Стили
- Добавить CSS переменные в `:root`:
  - `--primary-color: #667eea`
  - `--secondary-color: #ffffff`
  - `--bg-light: #f5f5f5`
  - `--text-light: #666`
- Стили для переключателя языков
- Стили для улучшенных брендов с иконками
- Стили для ассортимента товаров
- Адаптивность для мобильных устройств

## Технические требования

1. Все изменения должны быть обратно совместимы
2. Не удалять существующий функционал
3. Проверить, что форма обратной связи работает
4. Убедиться, что плавная прокрутка работает
5. Проверить адаптивность на мобильных устройствах

## Структура файлов

- `index.html` - основная страница
- `styles.css` - стили
- `script.js` - JavaScript с переводами и обработчиками

## Проверка

После изменений:
1. Открыть `index.html` в браузере
2. Проверить переключение языков
3. Проверить отображение всех секций
4. Проверить работу формы
5. Проверить адаптивность на мобильных
